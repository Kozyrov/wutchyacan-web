import React from 'react';
import { List } from '../../app/types';

interface TaskListManagerProps {
  taskLists: List[];
}

const TaskListManager = ({ taskLists }: TaskListManagerProps) => {
  return <div></div>;
};

export default TaskListManager;
